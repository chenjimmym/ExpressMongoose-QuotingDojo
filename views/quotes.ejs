<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Quoting Dojo</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>
        <h1>Here are the awesome quotes!</h1>
        <% for (var i in key){ %>
        <p>"<%= key[i].quote %>"</p>
        <% date = key[i].createdAt %>
        <p>- <%= key[i].name %> at <%= date.toUTCString() %></p>
        <p>-------------------------</p>
        <% } %>
    </body>
</html>